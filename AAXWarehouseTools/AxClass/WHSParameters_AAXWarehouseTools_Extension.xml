<?xml version="1.0" encoding="utf-8"?>
<AxClass xmlns:i="http://www.w3.org/2001/XMLSchema-instance">
	<Name>WHSParameters_AAXWarehouseTools_Extension</Name>
	<SourceCode>
		<Declaration><![CDATA[
/// <summary>
/// Extension of the <c>WHSParameters</c> table for the AAXWarehouseTools model. Provides the
/// label preview capture parameters, their defaults, and the capture auto-off logic.
/// </summary>
[ExtensionOf(tableStr(WHSParameters))]
public final class WHSParameters_AAXWarehouseTools_Extension
{
    // The size and density fields store inches and DPI; the accessors convert to millimeters and dots per millimeter
}
]]></Declaration>
		<Methods>
			<Method>
				<Name>aaxPreviewAutoOffDays</Name>
				<Source><![CDATA[
    /// <summary>
    /// Gets the capture auto-off window in days, defaulting to 14 when unset.
    /// </summary>
    /// <returns>The auto-off window in days.</returns>
    public int aaxPreviewAutoOffDays()
    {
        return this.AAXPreviewAutoOffDays > 0 ? this.AAXPreviewAutoOffDays : 14;
    }

]]></Source>
			</Method>
			<Method>
				<Name>aaxPreviewDefaultDpmm</Name>
				<Source><![CDATA[
    /// <summary>
    /// Gets the default render density in dots per millimeter, converted from the stored DPI
    /// (default 203).
    /// </summary>
    /// <returns>The default render density in dots per millimeter.</returns>
    public int aaxPreviewDefaultDpmm()
    {
        int dpi = this.AAXPreviewDefaultDpi > 0 ? this.AAXPreviewDefaultDpi : 203;
        return real2int(round(dpi / 25.4, 1.0));
    }

]]></Source>
			</Method>
			<Method>
				<Name>aaxPreviewDefaultWidthMm</Name>
				<Source><![CDATA[
    /// <summary>
    /// Gets the default label width in millimeters, converted from the stored inches (default 4).
    /// </summary>
    /// <returns>The default label width in millimeters.</returns>
    public real aaxPreviewDefaultWidthMm()
    {
        real widthIn = this.AAXPreviewDefaultWidthIn > 0 ? this.AAXPreviewDefaultWidthIn : 4;
        return widthIn * 25.4;
    }

]]></Source>
			</Method>
			<Method>
				<Name>aaxPreviewDefaultHeightMm</Name>
				<Source><![CDATA[
    /// <summary>
    /// Gets the default label height in millimeters, converted from the stored inches (default 6).
    /// </summary>
    /// <returns>The default label height in millimeters.</returns>
    public real aaxPreviewDefaultHeightMm()
    {
        real heightIn = this.AAXPreviewDefaultHeightIn > 0 ? this.AAXPreviewDefaultHeightIn : 6;
        return heightIn * 25.4;
    }

]]></Source>
			</Method>
			<Method>
				<Name>aaxPreviewRetentionDays</Name>
				<Source><![CDATA[
    /// <summary>
    /// Gets the attachment retention window in days used by the purge batch, defaulting to 14
    /// when unset.
    /// </summary>
    /// <returns>The retention window in days.</returns>
    public int aaxPreviewRetentionDays()
    {
        return this.AAXPreviewRetentionDays > 0 ? this.AAXPreviewRetentionDays : 14;
    }

]]></Source>
			</Method>
			<Method>
				<Name>aaxApplyPreviewDefaultsIfUnset</Name>
				<Source><![CDATA[
    /// <summary>
    /// Sets each unset label preview parameter to its default value.
    /// </summary>
    public void aaxApplyPreviewDefaultsIfUnset()
    {
        if (!this.AAXPreviewAutoOffDays)
        {
            this.AAXPreviewAutoOffDays = 14;
        }
        if (!this.AAXPreviewRetentionDays)
        {
            this.AAXPreviewRetentionDays = 14;
        }
        if (!this.AAXPreviewDefaultDpi)
        {
            this.AAXPreviewDefaultDpi = 203;   // DPI
        }
        if (!this.AAXPreviewDefaultWidthIn)
        {
            this.AAXPreviewDefaultWidthIn = 4;  // inches
        }
        if (!this.AAXPreviewDefaultHeightIn)
        {
            this.AAXPreviewDefaultHeightIn = 6; // inches
        }
    }

]]></Source>
			</Method>
			<Method>
				<Name>initValue</Name>
				<Source><![CDATA[
    /// <summary>
    /// Extends <c>initValue</c> to apply the label preview parameter defaults after the base
    /// implementation is called with <c>next</c>.
    /// </summary>
    public void initValue()
    {
        next initValue();
        this.aaxApplyPreviewDefaultsIfUnset();
    }

]]></Source>
			</Method>
			<Method>
				<Name>aaxInitPreviewAttachmentTurnsOff</Name>
				<Source><![CDATA[
    /// <summary>
    /// Sets the capture auto-off timestamp to the current time plus the configured window.
    /// </summary>
    public void aaxInitPreviewAttachmentTurnsOff()
    {
        this.AAXPreviewAttachmentTurnsOff = DateTimeUtil::addDays(
            DateTimeUtil::utcNow(),
            this.aaxPreviewAutoOffDays());
    }

]]></Source>
			</Method>
			<Method>
				<Name>aaxCanUpdateWHSParameters</Name>
				<Source><![CDATA[
    /// <summary>
    /// Indicates whether the parameters record may be updated by the auto-off logic.
    /// </summary>
    /// <returns>True if the record may be updated.</returns>
    private boolean aaxCanUpdateWHSParameters()
    {
        return true;
    }

]]></Source>
			</Method>
			<Method>
				<Name>aaxIsPreviewToWorkAttachmentEnabled</Name>
				<Source><![CDATA[
    /// <summary>
    /// Indicates whether label capture is enabled, honoring the auto-off window. An expired
    /// toggle is switched off under a pessimistic lock, and a missing timestamp initializes the
    /// window.
    /// </summary>
    /// <returns>True if label capture is currently enabled.</returns>
    public boolean aaxIsPreviewToWorkAttachmentEnabled()
    {
        if (this.AAXPreviewToWorkAttachment)
        {
            if (DateTimeUtil::utcNow() < this.AAXPreviewAttachmentTurnsOff)
            {
                return true;
            }

            boolean mustEnable = this.AAXPreviewAttachmentTurnsOff == utcDateTimeNull();

            if (this.aaxCanUpdateWHSParameters())
            {
                WHSParameters updateParameters;
                updateParameters.readPast(true);

                ttsbegin;
                select pessimisticlock firstonly updateParameters
                    where updateParameters.ParametersKey == this.ParametersKey
                       && updateParameters.AAXPreviewAttachmentTurnsOff == this.AAXPreviewAttachmentTurnsOff
                       && updateParameters.AAXPreviewToWorkAttachment == true;

                if (updateParameters)
                {
                    if (mustEnable)
                    {
                        updateParameters.aaxInitPreviewAttachmentTurnsOff();
                    }
                    else
                    {
                        updateParameters.AAXPreviewToWorkAttachment = NoYes::No;
                    }
                    updateParameters.update();
                }
                ttscommit;
            }

            return mustEnable;
        }

        return this.AAXPreviewToWorkAttachment;
    }

]]></Source>
			</Method>
			<Method>
				<Name>modifiedField</Name>
				<Source><![CDATA[
    /// <summary>
    /// Extends <c>modifiedField</c> to initialize the auto-off timestamp when the capture toggle
    /// is switched on. The base implementation is called with <c>next</c>.
    /// </summary>
    /// <param name = "_fieldId">The id of the modified field.</param>
    public void modifiedField(FieldId _fieldId)
    {
        next modifiedField(_fieldId);

        if (_fieldId == fieldNum(WHSParameters, AAXPreviewToWorkAttachment)
            && this.AAXPreviewToWorkAttachment)
        {
            this.aaxInitPreviewAttachmentTurnsOff();
        }
    }

]]></Source>
			</Method>
		</Methods>
	</SourceCode>
</AxClass>
