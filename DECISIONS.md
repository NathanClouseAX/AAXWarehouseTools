# DECISIONS.md — recorded Decision Rule applications (plan C-10)

| Date | DR | Decision |
|---|---|---|
| 2026-06-04 | WP-0.1 | Model Id = **896000929** (adjacent to AAXIntegrationOperations 896000928; verified unique across all `*\Descriptor\*.xml`; 895972610 rejected — owned by Microsoft HRPlatform). |
| 2026-06-04 | C-4/F36 | Label prefix `@AAXWarehouseTools:` (LabelFileId = AAXWarehouseTools); content file `AAXWarehouseTools.en-US.label.txt` (dot convention per sibling donor). |
| 2026-06-04 | REQ-R-1 | Renderer dimension policy clarified: `dpmm <= 0` always throws `ZplDimensionsMissingException` (density must come from caller/X++ fallback); `^PW`/`^LL` parsing only supplies width/height. |
| 2026-06-04 | III.7 | C# API adds X++-friendly `ZplRenderResult` (Count / GetPng(i)) because X++ cannot declare closed generic CLR types like `IList<byte[]>`. `RenderToPngList` retained for C# tests. |
| 2026-06-04 | DR-2 **TRIGGERED — overturns the earlier "not triggered" entry** | Build-time signing succeeded (CS8002 warning only), but the **.NET Framework runtime in the AOS enforces** what the compiler no longer does: a strong-named assembly cannot LOAD unsigned dependencies (`FUSION_E_SIGNATURE_CHECK_FAILED` 0x80131044 on `BinaryKits.Zpl.Viewer, PKT=null`, hit live in Feature A preview; net8 tests passed because modern .NET dropped the rule). **Shipped unsigned: `PublicKeyToken=null`** (sibling precedent F29/F35); AxReference updated. `AtomicAx.snk` retained in repo unused. |
| 2026-06-04 | WP-0.2 | Lib multi-targets **net472 (AOS-loaded) + net8.0 (test-referenced)**. Custom MSBuild targets `AAXCopyToModelBin` + `AAXStageNatives` (classic net472 does not auto-copy NuGet native assets — without staging, `DllNotFoundException` at runtime). |
| 2026-06-04 | WP-0.2 | Extra transitive DLLs shipped beyond plan list: `SixLabors.ImageSharp.dll` (ZXing.Net 0.16.11 dep), `zxing.presentation.dll` (unused WPF satellite, harmless), five `System.*` BCL shims. Resolved versions: BinaryKits.Zpl.Viewer 1.3.1 / Label 3.3.1, SkiaSharp 3.119.1, HarfBuzzSharp 8.3.1.2, ZXing.Net 0.16.11. |
| 2026-06-04 | U-2 **sharpened** | ZXing identity collision is REAL: our ZXing.Net 0.16.11 ships `zxing, PublicKeyToken=4e88037ac681fe60` — **same simple name AND same PKT as ER's 0.16.5.0**, version-only difference. The plan's "different identity → independent binding" assumption is false. WP-0.5 probe MUST log which `zxing` binds in-AOS; a binding redirect to 0.16.11 is the likely remedy if ER's wins. |
| 2026-06-04 | WP-2.3 | `LayoutId` left blank at capture time: the routing-line layout id is not available at either CoC seam without a third hook (rejected — seam-minimalism). Attachment filename omits the empty segment. |
| 2026-06-04 | DR-6 | Staging insert in caller's transaction (rolled-back print discards its row); `UserConnection` alternative documented in plan §7.3/F21. |
| 2026-06-05 | WHSParameters defaults + imperial units | Parameter fields now carry defaults and use **inches/DPI** (user request): auto-off **14 days**, retention **14 days** (was 30), density **203 DPI**, width **4 in**, height **6 in**. Implemented schema-free (no rename, no DB sync): the `*WidthMm/*HeightMm/*Dpmm` fields now **store inches/DPI**; the accessors convert to the renderer's mm/dpmm (`inch×25.4`; `dpmm=round(DPI/25.4)` → 203→8). Defaults applied via `WHSParameters.initValue()` CoC (new records) + a `WHSParameters` form `init()` CoC that lazily stamps the single record (existing records), Edit-access-gated. "AAXPreviewAttachmentTurnsOff = 14" interpreted as the auto-off **days** field (the TurnsOff timestamp is computed, not a static default). Fields renamed 2026-06-05 (dev, drop/add OK) to match units: `AAXPreviewDefaultDpmm`→`AAXPreviewDefaultDpi`, `…WidthMm`→`…WidthIn`, `…HeightMm`→`…HeightIn` (+ matching `@AAXWarehouseTools:PreviewDefault*` label keys). **Requires DB sync** (drops the old columns, adds the new). The converting accessor METHODS keep their `…Dpmm/…Mm` names because they return the renderer's native mm/dpmm (the fields store inches/DPI; the methods convert). |
| 2026-06-05 | Preview sizing — honor `^PW`/`^LL`; default 4×6; size-override box | **Bug fixed:** the wrapper sent `renderToPngList(zpl,0,0,0)`; the C# `dpmm<=0` guard threw before the `^PW`/`^LL` parser ran, so the retry forced the parameter size and **every preview was squashed to 4×2, ignoring each label's declared size.** Now `dpmm` is resolved up front and passed on the first call, so the label's own `^PW`/`^LL` wins; only a label declaring no size falls back — **default changed 4×2 → 4×6 in** (`aaxPreviewDefaultHeightMm` 50.8→152.4; width stays 101.6). User-approved design: a "Label size" group on the preview dialog (Override checkbox + Width/Height in inches + DPI, default 4/6/203) overrides when ticked; inches→mm ×25.4, DPI→dpmm `real2int(round(dpi/25.4,1.0))`. Override threaded through buildContext/buildContextFromSource/buildTemplateContext/renderFromSource and persisted on the context. This supersedes the plan's locked "4×2 default" (design doc decision 2026-06-03). 6 new `@AAXWarehouseTools:` label keys. |
| 2026-06-05 | Editable+savable source ZPL | Preview dialog now edits the RAW SOURCE ZPL (with `$tokens$`/`{{templates}}`) — never the translated output — and saves it back: `WHSDocumentRoutingLayout.ZPL` directly, or the active `WHSLabelLayoutVersion.ZPL` row (verified editable in place — that version table has only ZPL/LabelLayoutId/VersionNumber, no lock/status field, and MS's own WHSLabelLayout form edits it live). "Preview edits" re-renders unsaved text; "Save to layout" persists. `saveSourceToLayout` returns `container[boolean,messageKey]` (X++ has no `str&` by-ref). **Security: runtime `hasTableAccess(table, AccessType::Edit)` gate** — no dedicated save privilege added: D365 privileges grant table access via EntryPoints (menu items), and the save is a dialog button, not a menu item; a direct table-grant schema isn't donor-evidenced (C-6), and the runtime gate already lets standard layout-maintainers save while denying read-only preview users. |
| 2026-06-04 | **WP-0.5 GATE PASSED (V3/HG-1)** | In-AOS probe ALL GREEN: plain + barcode (`^BC`/ZXing) ZPL rendered, `LBLPREV` DocuType created (with `DocuActionArchive` — F26 corrected: `DocuActionFile` is abstract), PNG attached (DocuRef 68719492672 on WHSParameters — removable via the attachments UI). Identity log shows ONLY `AtomicAx.Zpl.Render` as a renderer assembly → U-2 ER-coexistence dissolved by the merge. Feature A preview confirmed working live by the user. Probe deleted per C-7; model recompiles clean. Root cause of recurring bin pollution: rnrproj `ProjectReference` `Private=True` redeployed unmerged payload (incl. x86 native) on every VS build — now `Private=False` + `ReferenceOutputAssembly=false`. |
| 2026-06-04 | **U-1 FINAL: single standalone DLL** | After exhausting load-fabric fixes (unsigned, preloader, AssemblyResolve, x86→x64 arch, stage-beside-managed) the payload was restructured per user direction: **ILRepack merges every managed dep (internalized) into `AtomicAx.Zpl.Render.dll`; win-x64 natives ship as embedded resources, self-extracted beside the assembly (or %TEMP%) at first use.** Eliminates Fusion resolution for the whole renderer stack, all version/identity collisions with ElectronicReporting, and any multi-file deployment-correctness dependency (Tier-2 packaging trivially carries ONE bin file). Validated standalone: lone DLL in empty dir, 64-bit .NET Framework, plain + barcode render OK, zero non-framework references remain. Model bin scrubbed of the old 16-DLL payload. |
| 2026-06-04 | C-3 sharpened | X++ CoC `next` must be called **unconditionally** (compiler-enforced) — the plan's gate-then-early-return skeleton was illegal. `printDocument` CoC now always opens the scope with an `active` flag carrying the REQ-B-5 gate result; inactive scope stages nothing. |
| 2026-06-04 | C-2 | `WHSRecordDeletionCommitter` is `internal` → purge replicates chunked deletion inline (per-row `delete()`); `ReqTransientSqlConnectionErrorHandlingHelper` also internal → retry gated on `xSession::currentRetryCount()`. |
| 2026-06-04 | WP-2.6 | Privilege grant shape = CRUD block (donor `WHSWorkCreateHistoryPurgeProcess`), not `Invoke` — no MS precedent for Invoke-grants on class action menu items. Menu = `WHS`: render batch → `PeriodicTasks`, purge → `Cleanup` (alongside `WHSWorkCreateHistoryPurge`). |
| 2026-06-04 | REQ-A-3 | Seed dialog deferred to v2; v1 = auto-resolve most-recent `WHSLicensePlateLabel` + always-attached sample map (badged Live/Sample). |
| 2026-06-04 | U-5 **RESOLVED** | URL-bound image proven: `FormImageControl.imageLocation(SysImageLocation::URL)` + `.normalImage(url)` (donors `RetailMediaImageViewer`, `EcoResProductImage`). Dialog = Pattern Custom + Style Dialog + DialogSize Large. |
| 2026-06-04 | REQ-A-7 partial | Copy-ZPL button omitted (no clipboard API provable from MS donors) — read-only multiline selectable text is the copy affordance. Collapsible group not in this metadata schema → plain captioned group (ZPL always visible beside image). |
| 2026-06-04 | WP-2.4 | Filename timestamp built from `DateTimeUtil` components + `num2Str0` (locale-independent `yyyyMMdd_HHmmss`); row claim via `select forupdate where Status==Pending` (concurrent-batch safe); dedup via attachment-service notes-hash (staging `existsForWorkAndHash` unused by batch). |
| 2026-06-04 | Form ext | Params group anchored to group `Work` (not `WorkTabPage` — its ToolbarFields pattern rejects direct group children). |
